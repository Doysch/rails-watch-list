 <div class="container">
  <h1>Watch List</h1>
  <div class="card-wrapper">
    <% @lists.each do |list| %>
       <%= link_to list.name, list_path(list) %>
       <br>
       <% end %>
  </div>
  <br>
  <%= link_to "Add new list", new_list_path %>
</div>
